# ------------------------------------------------------------------
# requirements.txt - Hệ thống Quản lý Tour Du lịch Tích hợp AI
# ------------------------------------------------------------------
# LƯU Ý (điều chỉnh để tương thích Python 3.13 - không có wheel pre-built):
#   - psycopg2-binary 2.9.9  -> 2.9.10  (2.9.9 không có bản build cho CPython 3.13)
#   - sqlalchemy      2.0.25 -> 2.0.36  (2.0.25 lỗi "TypingOnly AssertionError" trên 3.13)
#   - pydantic        2.5.3  -> 2.10.6  (pydantic-core 2.5.3 không có wheel cho 3.13)
#   - bcrypt 4.0.1 được pin kèm vì passlib 1.7.4 không tương thích bcrypt >= 4.1
# ------------------------------------------------------------------

fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic==2.10.6
python-dotenv==1.0.0
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
# google-generativeai 0.8.4: bản mới hỗ trợ response_schema (Structured Outputs),
# request_options (timeout) và tương thích Python 3.13 + API key dạng AQ.
google-generativeai==0.8.4
pytest==7.4.3
